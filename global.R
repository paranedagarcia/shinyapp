#
#if(!exists("comunas")) {
#    showNotification("Cargando comunas ...")
#    comunas <- geojsonio::geojson_read("comunas.json", what = "sp")
#}

#if(!exists("regiones")) {
#    showNotification("Cargando regiones ...")
#    regiones <- geojsonio::geojson_read("regiones.json", what = "sp")
#}
